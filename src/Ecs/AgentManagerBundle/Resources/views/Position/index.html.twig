<h1>Position list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Position_name</th>
            <th>Position_default_roles</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('agentmanager_empositions_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.positionname }}</td>
            <td>{{ entity.positiondefaultroles }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('agentmanager_empositions_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('agentmanager_empositions_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('agentmanager_empositions_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
